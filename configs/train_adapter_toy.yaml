seed: 7
benchmark_root: data/toy_benchmark
records_file: records/train.jsonl
work_dir: work_dirs/adapter_toy
batch_size: 32
num_workers: 0
epochs: 2
lr: 0.001
weight_decay: 0.0001
log_every: 5
model:
  dino_backbone: toy_dino
  clip_image_encoder: toy_clip
  embed_dim: 64
  adapter_hidden: 128
  image_size: 32
